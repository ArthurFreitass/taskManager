# taskManager
TaskManager is a console-based application that lets users manage personal tasks. It supports multiple users, task categories, priorities, due dates, and two types of tasks (simple and recurring). All data is persisted to plain text files so that information is not lost between runs.
